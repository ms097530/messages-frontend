Interactive comments section notes

Structure
- section.comments-container
	- div.comments
	- div.comment-form

- data is provided in `data.json` if don't want to bother with server
- comments from the basic comment form have a `replies` attribute
	- replies are similarly structured but without a `replies` array
		- have a `replyingTo` attribute with the username of who the reply is directed at but no `replies` attribute

can assign random userId to user when first visiting page to simulate being an already existing user